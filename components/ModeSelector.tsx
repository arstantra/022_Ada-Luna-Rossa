// ModeSelector è stato sostituito da ModePills (pill inline, nessun dropdown).
// Questo file è un alias per retrocompatibilità — importa direttamente ModePills.
export { default } from './ModePills';
